import React, { Component } from "react";

class StartButton extends Component {
  render() {
    return (
      <button {...this.props}>
        Start Here
      </button>
    );
  }
}

export default StartButton;
